[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "voxgig-sdk-placeholder-image"
version = "0.0.1"
description = "Unofficial generated Python SDK for the Placeholder Image public API. Not affiliated with or endorsed by the upstream API provider."
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
keywords = ["voxgig", "sdk", "generated-sdk", "openapi", "api-client", "placeholder-image"]
dependencies = [
    "requests>=2.33",
]

[project.urls]
Homepage = "https://github.com/voxgig-sdk/placeholder-image-sdk"
Repository = "https://github.com/voxgig-sdk/placeholder-image-sdk"
Issues = "https://github.com/voxgig-sdk/placeholder-image-sdk/issues"

# ONE package. core/entity/feature/utility were previously top-level packages;
# those names are real PyPI distributions and common scratch filenames, and
# Python searches the working directory first, so any of them beside the
# caller silently shadowed the SDK's own. They now live inside
# placeholderimage_sdk/ where nothing can reach them.
[tool.setuptools.packages.find]
include = ["placeholderimage_sdk*"]

# Ship the PEP 561 py.typed marker(s) so the inline type hints reach consumers.
[tool.setuptools.package-data]
"*" = ["py.typed"]
